# CONV 进制转换

## 问题

`CONV()` 是什么？

## 分类

SQL函数

## 回答

`CONV(N, from_base, to_base)` 用来把数字从一种进制转换成另一种进制。

语法：

```sql
CONV(N, from_base, to_base)
```

含义：

- `N`：要转换的数字
- `from_base`：原来的进制
- `to_base`：目标进制

## 总结

`CONV()` 主要用于进制转换，例如二进制、十进制、十六进制之间的转换。

