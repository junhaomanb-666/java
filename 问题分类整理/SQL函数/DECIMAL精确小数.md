# DECIMAL 精确小数

## 问题

`DECIMAL(M,D)` 是什么？

## 分类

SQL函数

## 回答

`DECIMAL(M,D)` 表示精确小数。

- `M`：总位数
- `D`：小数位数

例如：

```sql
DECIMAL(8,2)
```

表示最多 8 位数字，其中 2 位是小数。

## 总结

`DECIMAL` 适合存储金额、价格、分数等需要精确计算的小数。

