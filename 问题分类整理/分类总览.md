# 杂项问题分类总览

来源文件：

- `D:\Documents\Documents Of Java\杂项\杂项问题1.md`
- `D:\Documents\Documents Of Java\杂项\杂项问题2.md`

## 分类结果

### SQL查询结构

- `WITH` / CTE 公共表表达式
- `ALL`、`ANY`、`SOME` 子查询比较

### SQL窗口函数

- `ROW_NUMBER()`
- `ROW_NUMBER()`、`RANK()`、`DENSE_RANK()` 的区别

### SQL函数

- `CONV()` 进制转换
- `CURDATE()`、`NOW()`、`DATE_FORMAT()`、`DATE()`、`DATE_SUB()` 日期函数
- `COALESCE()` 空值处理
- `DECIMAL(M,D)` 精确小数

### SQL视图

- 视图与基表
- `WITH` 与 `VIEW` 的区别

### MySQL存储过程与异常

- `SIGNAL`
- `RESIGNAL`
- 异常处理器 `HANDLER`
- 事务回滚 `ROLLBACK`

