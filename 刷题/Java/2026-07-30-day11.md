# Java 刷题总结 - 2026-07-30 day11

来源：[练习day11.md](D:/Documents/Documents%20Of%20Java/刷题/练习day11.md)

原始材料归档：[练习day11.md](D:/Workspace/刷题/原始材料/练习day11.md)

## 今日概览

- Java 题：5 道
- 已知错题：4 道，1 道未记录你的答案
- 高频考点：构造期间多态调用、`synchronized` 锁对象、抽象类与接口、`final` 类、反射

## Java-001 构造方法中的多态调用

**题目：**

下面代码的输出是什么？

```java
public class Base {
    private String baseName = "base";

    public Base() {
        callName();
    }

    public void callName() {
        System.out.println(baseName);
    }

    static class Sub extends Base {
        private String baseName = "sub";

        public void callName() {
            System.out.println(baseName);
        }
    }

    public static void main(String[] args) {
        Base b = new Sub();
    }
}
```

**选项：**

- A. `null`
- B. `sub`
- C. `base`

**正确答案：** A

**你的答案：** B

**原文件解析：**

首先，需要明白类的加载顺序：

1. 父类静态代码块，包括静态初始化块、静态属性，但不包括静态方法
2. 子类静态代码块，包括静态初始化块、静态属性，但不包括静态方法
3. 父类非静态代码块，包括非静态初始化块、非静态属性
4. 父类构造函数
5. 子类非静态代码块，包括非静态初始化块、非静态属性
6. 子类构造函数

其中，类中静态块按照声明顺序执行，并且 1 和 2 不需要调用 `new` 类实例的时候就执行了，也就是类加载到方法区的时候执行。

其次，需要理解子类覆盖父类方法的问题，也就是方法重写实现多态问题。

`Base b = new Sub();` 是多态的一种表现形式，声明是 `Base`，实现是 `Sub` 类。可以理解为 `b` 编译时表现为 `Base` 类特性，运行时表现为 `Sub` 类特性。

当子类覆盖了父类的方法后，父类的方法已经被重写。题中父类初始化调用的方法为子类实现的方法，子类实现的方法中调用的 `baseName` 为子类中的私有属性。

由类初始化顺序可知，此时只执行到父类构造函数，子类非静态代码块和初始化步骤还没有到，子类中的 `baseName` 还没有被初始化。所以此时 `baseName` 为空，因此输出 `null`。

执行 `Base b = new Sub();` 时由于多态，`b` 编译时表现为 `Base` 类特性，运行时表现为 `Sub` 类特性。不管是哪种状态都会调用 `Base` 构造器执行 `callName()` 方法。

而此时子类尚未初始化，执行完父类构造器后才会开始执行子类。如果有重写方法就执行子类方法，没有再去父类寻找；如果把父类 `callName()` 改为 `callName2()`，则会输出 `base`。

流程：

```text
分配对象内存并赋默认值
-> 进入 Sub 构造流程
-> 调用 super()
-> 初始化 Base 实例字段
-> 执行 Base 构造方法
-> 动态调用 Sub.callName()
-> Sub.baseName 仍是 null
-> 输出 null
-> 初始化 Sub 实例字段为 "sub"
-> 执行 Sub 构造方法体
```

**我的补充解析：**

这题的危险点是“父类构造方法里调用可重写方法”。虽然调用发生在父类构造阶段，但动态绑定仍然会调用子类重写后的 `callName()`。此时子类字段还没执行显式初始化，引用类型默认值是 `null`。

**考点：**

- 初始化顺序
- 构造方法
- 方法重写
- 动态绑定
- 字段默认值

**易错点：**

- 构造方法中调用可重写方法可能触发子类未初始化状态
- 子类字段显式初始化发生在父类构造方法之后

## Java-002 `synchronized` 锁对象

**题目：**

关于下面一段代码，以下说法正确的是？

```java
public class Test {
    private synchronized void a() {
    }

    private void b() {
        synchronized (this) {
        }
    }

    private synchronized static void c() {
    }

    private void d() {
        synchronized (Test.class) {
        }
    }
}
```

**选项：**

- A. 同一个对象，分别调用方法 `a` 和 `b`，锁住的是同一个对象
- B. 同一个对象，分别调用方法 `a` 和 `c`，锁住的是同一个对象
- C. 同一个对象，分别调用方法 `b` 和 `c`，锁住的不是同一个对象
- D. 同一个对象，分别调用方法 `a`、`b`、`c`，锁住的是同一个对象

**正确答案：** A、C

**你的答案：** 未记录

**原文件解析：**

修饰非静态方法，锁的是 `this` 对象。

举例：

```java
Test t1 = new Test();
Test t2 = new Test();
```

如果线程 1 执行：

```java
t1.b();
```

它锁住的是：

```text
t1 对象
```

此时线程 2 也执行：

```java
t1.b();
```

线程 2 必须等待，因为它也想获得 `t1` 的锁。

但是线程 2 执行：

```java
t2.b();
```

通常不需要等待，因为 `t1` 和 `t2` 是两个不同对象，拥有不同的锁。

```text
t1 的锁 != t2 的锁
```

修饰静态方法，锁的是 class 对象。

举例：

```java
Test t1 = new Test();
Test t2 = new Test();
```

线程 1 通过 `t1` 执行锁 `Test.class` 的代码：

```java
t1.d();
```

线程 2 通过 `t2` 执行：

```java
t2.d();
```

即使是两个不同对象，线程 2 也要等待，因为它们锁的都是：

```text
Test.class
```

也就是说：

```text
t1 对应的类锁 = t2 对应的类锁
```

**我的补充解析：**

`a()` 是非静态同步方法，等价于 `synchronized(this)`；`b()` 明确写了 `synchronized(this)`，所以同一个对象调用 `a` 和 `b` 锁相同。

`c()` 是静态同步方法，锁的是 `Test.class`。`b()` 锁 `this`，`c()` 锁 `Test.class`，不是同一个锁。

**考点：**

- `synchronized` 非静态方法
- `synchronized` 静态方法
- 对象锁
- 类锁

**易错点：**

- 非静态同步方法锁 `this`
- 静态同步方法锁 `Class` 对象

## Java-003 抽象类和接口

**题目：**

关于抽象类和接口叙述正确的是？

**选项：**

- A. 抽象类和接口都能实例化
- B. 抽象类不能实现接口
- C. 抽象类方法的访问权限默认都是 `public`
- D. 接口方法的访问权限默认都是 `public`

**正确答案：** D

**你的答案：** 未记录

**原文件解析：**

### 抽象类

特点：

1. 抽象类中可以有构造方法。
2. 抽象类中可以存在普通属性、方法、静态属性和方法。
3. 抽象类中可以存在抽象方法。
4. 如果一个类中有一个抽象方法，那么当前类一定是抽象类；抽象类中不一定有抽象方法。
5. 抽象类中的抽象方法，需要有子类实现；如果子类不实现，则子类也需要定义为抽象的。
6. 抽象类不能被实例化，抽象类和抽象方法必须被 `abstract` 修饰。
7. 抽象类中的方法访问修饰符默认是 `default`。
8. 抽象类可以实现接口，而且不需要实现接口中的所有方法。

关键字使用注意：抽象类中的抽象方法，其前有 `abstract` 修饰，不能用 `private`、`static`、`synchronized`、`native` 访问修饰符修饰。

### 接口

1. 在接口中只有方法的声明，没有方法体。
2. 在接口中只有常量，因为定义的变量在编译的时候都会默认加上 `public static final`。
3. 在接口中的方法，永远都被 `public` 修饰。
4. 接口中没有构造方法，也不能实例化接口的对象，所以接口不能继承类。
5. 接口可以实现多继承。
6. 接口中定义的方法都需要有实现类来实现，如果实现类不能实现接口中的所有方法，则实现类定义为抽象类。
7. 接口可以继承接口，用 `extends`。
8. 接口中方法访问修饰符默认是 `public abstract`，但是 JDK 1.8 之后接口中可以有 `static` 方法或者 `default` 方法，这两种方法必须有方法体。

**我的补充解析：**

A 错误，抽象类和接口都不能直接实例化。

B 错误，抽象类可以实现接口，并且可以不实现接口所有方法。

C 错误，抽象类中的普通方法如果不写访问修饰符，默认是包访问权限。

D 正确，接口中的抽象方法默认是 `public abstract`。

**考点：**

- 抽象类
- 接口
- 默认访问权限
- `public abstract`

**易错点：**

- 抽象类方法默认不是 `public`
- 接口方法默认是 `public abstract`

## Java-004 `final` 类型

**题目：**

以下类型为 `Final` 类型的为？

**选项：**

- A. `HashMap`
- B. `StringBuffer`
- C. `String`
- D. `Hashtable`

**正确答案：** B、C

**你的答案：** C、D

**原文件解析：**

通过阅读源码可以知道，`String` 与 `StringBuffer` 都是通过字符数组实现的。

其中 `String` 的字符数组是 `final` 修饰的，所以字符数组不可以修改。

`StringBuffer` 的字符数组没有 `final` 修饰，所以字符数组可以修改。

`String` 与 `StringBuffer` 都是 `final` 修饰，只是限制它们所存储的引用地址不可修改。

至于地址所指内容能不能修改，则需要看字符数组可不可以修改。

**我的补充解析：**

本题问“类型为 Final 类型”，也就是类本身是否被 `final` 修饰。`String` 和 `StringBuffer` 都是 `final class`，不能被继承。

但“类是 final”不等于“对象内容不可变”。`String` 不可变；`StringBuffer` 虽然类是 final，但对象内部字符内容可变。

**考点：**

- `final class`
- `String`
- `StringBuffer`
- 可变对象与不可变对象

**易错点：**

- `final` 类不能被继承
- `final` 类不一定代表对象内容不可变

## Java-005 Java 反射机制

**题目：**

Java 反射机制主要提供了以下哪些功能？

**选项：**

- A. 在运行时判断一个对象所属的类
- B. 在运行时构造一个类的对象
- C. 在运行时判断一个类所具有的成员变量和方法
- D. 在运行时调用一个对象的方法

**正确答案：** A、B、C、D

**你的答案：** A、D

**原文件解析：**

普通的 Java 对象是通过 `new` 关键字把对应类的字节码文件加载到内存，然后创建该对象的。

反射是通过一个名为 `Class` 的特殊类，用 `Class.forName("className")` 得到类的字节码对象，然后用 `newInstance()` 方法在虚拟机内部构造这个对象，针对无参构造函数。

也就是说，反射机制让我们可以先拿到 Java 类对应的字节码对象，然后动态地进行任何可能的操作。选项都是反射的功能。

**我的补充解析：**

反射的核心能力是运行期操作类型信息：识别类、创建对象、读取字段/方法/构造器信息、调用方法、访问成员变量等。

现代 Java 中更推荐使用：

```java
Class<?> clazz = Class.forName("com.example.User");
Object obj = clazz.getDeclaredConstructor().newInstance();
```

而不是直接使用已经较老的 `Class.newInstance()`。

**考点：**

- 反射
- `Class.forName()`
- 运行时类型信息
- 动态创建对象
- 动态调用方法

**易错点：**

- 反射不只是调用方法，也可以创建对象、查看字段和方法信息

## 今日错题回炉

| 编号 | 正确答案 | 你的答案 | 错因 | 复习动作 |
| --- | --- | --- | --- | --- |
| Java-001 | A | B | 忽略父类构造期间动态绑定到子类方法 | 手推初始化流程 |
| Java-004 | B、C | C、D | 混淆 final 类和不可变对象 | 查 `String`、`StringBuffer` 类声明 |
| Java-005 | A、B、C、D | A、D | 反射能力理解不完整 | 列出反射的 4 类常用能力 |
